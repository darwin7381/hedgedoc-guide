---
title: n8n Workflow 範本完整指南
---

# n8n Workflow 範本完整指南

使用預先配置好的 workflow 範本，快速整合 HedgeDoc + Token Manager Gateway

---

## 📦 範本下載

**範本檔案**：[hedgedoc-create-and-read-template.json](https://raw.githubusercontent.com/darwin7381/hedgedoc-guide/main/workflows/hedgedoc-create-and-read-template.json)

**GitHub Raw URL**（可直接在 n8n 中 Import from URL）：
```
https://raw.githubusercontent.com/darwin7381/hedgedoc-guide/main/workflows/hedgedoc-create-and-read-template.json
```
專案路徑：
```
../../workflows/hedgedoc-create-and-read-template.json
```

**相關文檔**：
- [workflows/README.md](../../workflows/README.md) - workflows 目錄說明
- [n8n-setup-guide.md](../n8n-setup-guide.md) - n8n 手動設置指南
- [hedgedoc-quick-start.md](../hedgedoc-quick-start.md) - curl 指令快速參考
- [04-troubleshooting/4.4-n8n-hedgedoc-redirect-issue.md](../04-troubleshooting/4.4-n8n-hedgedoc-redirect-issue.md) - 問題排查

---

## ⚡ 快速開始（3 步驟）

### 步驟 1：下載並匯入範本

**方法 A**：從本地文件匯入
1. 打開 `workflows/hedgedoc-create-and-read-template.json`
2. 複製全部內容
3. 在 n8n 中：右上角「⋯」→「Import from File」
4. 貼上 JSON 內容 → 點擊「Import」

**方法 B**：使用 curl 下載
```bash
curl -o hedgedoc-workflow.json \
  https://raw.githubusercontent.com/darwin7381/hedgedoc-guide/main/workflows/hedgedoc-create-and-read-template.json
```

**方法 C**：直接從 URL 匯入（最快）
1. 在 n8n 中：右上角「⋯」→「Import from URL」
2. 貼上 Raw URL
3. 點擊「Import」

### 步驟 2：設置你的 Token

找到 **「Content Parameter」** 節點：
1. 點擊節點打開編輯
2. 找到 `token_manager_key` 欄位
3. 將 `YOUR_TOKEN_HERE` 改為你的 Token

**推薦方式**（使用環境變數）：
```javascript
={{ $env.HEDGEDOC_TOKEN }}
```

### 步驟 3：執行測試

1. 點擊「**Execute workflow**」
2. 查看結果 ✅

**完成！** 🎉

---

## 📝 範本說明

### 功能清單

- ✅ 透過 Token Manager Gateway 創建 HedgeDoc 筆記
- ✅ 自動提取 Note ID
- ✅ 生成所有訪問 URL（編輯、雙開、只讀等）
- ✅ 讀取筆記內容
- ✅ 獲取筆記元數據

### Workflow 結構

```
[Manual Trigger] - 手動觸發
     ↓
[Content Parameter] - 設置 Token 和筆記內容
     ↓
[創建筆記] - POST /new（已設置正確的 redirect 處理）
     ↓
[整理新 markdown 文件輸出參數] - 提取 Note ID 和生成各種 URL
     ↓
     ├─ [讀取 markdown 文件內容] - GET /download
     ├─ [讀取元數據] - GET /info
     └─ [各版本 view] - 整理編輯/預覽 URLs
```

### 包含的節點（共 7 個）

1. **Manual Trigger** - 手動觸發
2. **Content Parameter** - 設置 Token 和內容
3. **創建筆記** - POST /new（已設置正確的 redirect 處理）
4. **整理新 markdown 文件輸出參數** - 提取 Note ID 和生成 URLs
5. **讀取 markdown 文件內容** - GET /download
6. **讀取元數據** - GET /info
7. **各版本 view** - 整理編輯/預覽 URLs

---

## 📊 範本輸出說明

### 「整理新 markdown 文件輸出參數」節點輸出

```json
{
  "noteId": "AQY5Z-KpQxyoK4JAOtTfcw",
  
  // Gateway URLs（透過 Token Manager）
  "gatewayUrl": "https://api-gateway.cryptoxlab.workers.dev/api/hedgedoc/AQY5Z-KpQxyoK4JAOtTfcw",
  "downloadUrl": "https://api-gateway.cryptoxlab.workers.dev/api/hedgedoc/AQY5Z-KpQxyoK4JAOtTfcw/download",
  "infoUrl": "https://api-gateway.cryptoxlab.workers.dev/api/hedgedoc/AQY5Z-KpQxyoK4JAOtTfcw/info",
  
  // 直接訪問 HedgeDoc URLs
  "directUrl": "https://md.blocktempo.ai/AQY5Z-KpQxyoK4JAOtTfcw",
  "editUrl": "https://md.blocktempo.ai/AQY5Z-KpQxyoK4JAOtTfcw?edit",
  "bothUrl": "https://md.blocktempo.ai/AQY5Z-KpQxyoK4JAOtTfcw?both",
  "viewUrl": "https://md.blocktempo.ai/AQY5Z-KpQxyoK4JAOtTfcw?view",
  
  "createdAt": "2025-11-09T10:15:33.123Z"
}
```

### URL 用途說明

| URL 類型 | 用途 | 何時使用 |
|---------|------|---------|
| `noteId` | 筆記 ID | 用於後續 API 調用 |
| `gatewayUrl` | Gateway 訪問 | 需要透過 Token Manager 訪問時 |
| `downloadUrl` | 下載 Markdown | 需要純文字內容時 |
| `infoUrl` | 獲取元數據 | 需要標題、時間等資訊時 |
| `directUrl` | 直接網頁訪問 | 分享給他人查看（預設模式） |
| `editUrl` | 整頁編輯模式 | 需要專注編輯時 |
| `bothUrl` | 雙開編輯模式 | 需要邊寫邊看預覽時 |
| `viewUrl` | 只讀模式 | 只需查看不需編輯時 |

---

## 🔧 範本設置詳解

### 必須設置的選項

**在「創建筆記」節點的 Options 中**：

```
Redirects:
  ☐ Follow Redirects (必須關閉)

Response:
  ☑ Include Response Headers and Status (必須開啟)
  ☑ Never Error (必須開啟)
```

**這些設置已包含在範本中**，但如果你修改了節點，請確保這些選項正確。

### 為什麼需要這些設置？

| 設置 | 作用 | 不設置的後果 |
|------|------|-------------|
| Follow Redirects = OFF | 不自動跟隨 redirect | 返回 HTML 頁面 |
| Include Response Headers and Status = ON | 返回完整響應（包含 headers） | 無法提取 Location |
| Never Error = ON | 允許非 2xx 狀態碼 | 302 被視為錯誤 |

---

## 💡 自定義範本

### 修改筆記內容

在「Content Parameter」節點中：

```javascript
{
  "Markdown Article": "# 你的標題\n\n你的內容...",
  "token_manager_key": "YOUR_TOKEN_HERE"
}
```

### 使用動態內容

如果你想從上一個節點獲取內容：

```javascript
{
  "Markdown Article": "={{ $json.articleContent }}",
  "token_manager_key": "={{ $env.HEDGEDOC_TOKEN }}"
}
```

### 使用環境變數（推薦）

在 n8n Settings → Variables 中設置：

```
Name: HEDGEDOC_TOKEN
Value: ntk_your_actual_token_here
```

然後在範本中：

```javascript
"token_manager_key": "={{ $env.HEDGEDOC_TOKEN }}"
```

### 添加錯誤處理

在「創建筆記」節點後添加 **IF** 節點：

```javascript
// 檢查是否成功
{{ $json.statusCode === 302 && $json.headers.location }}
```

---

## 🎯 實際應用場景

### 場景 1：自動發布文章到 HedgeDoc

```
[Webhook Trigger] - 接收新文章
     ↓
[Code] - 轉換為 Markdown 格式
     ↓
[導入此範本的創建筆記節點] - 創建 HedgeDoc 筆記
     ↓
[Slack] - 發送通知（包含 editUrl）
```

**Slack 訊息範例**：
```
新筆記已創建！
📝 編輯連結：{{$json.editUrl}}
👀 預覽連結：{{$json.viewUrl}}
```

### 場景 2：RSS 轉 HedgeDoc

```
[RSS Feed Trigger] - 監控 RSS
     ↓
[Filter] - 篩選新文章
     ↓
[Code] - 格式化為 Markdown
     ↓
[導入此範本] - 創建 HedgeDoc 筆記
     ↓
[Database] - 記錄 Note ID
```

### 場景 3：批量備份

```
[Cron Trigger] - 每日執行
     ↓
[Database] - 讀取所有 Note IDs
     ↓
[Loop] - 遍歷每個 Note
     ↓
[HTTP Request] - 讀取內容（使用範本的讀取節點配置）
     ↓
[Write File] - 保存到本地
```

---

## 🧪 驗證清單

### 匯入後檢查

匯入範本後，執行測試前檢查：

- [ ] 「創建筆記」節點的 URL 正確
- [ ] 「創建筆記」節點的 Options 設置正確：
  - [ ] Follow Redirects = OFF
  - [ ] Never Error = ON
  - [ ] Include Response Headers and Status = ON
- [ ] 「Content Parameter」節點的 Token 已替換
- [ ] 所有連線都正確（沒有斷開的線）
- [ ] 執行測試成功

### 預期結果

執行成功後：

1. **「創建筆記」節點**：
   - statusCode: 302
   - headers.location: 包含新筆記 URL

2. **「整理參數」節點**：
   - 輸出 9 個欄位（noteId, 各種 URLs, createdAt）

3. **「讀取內容」節點**：
   - 返回完整的 Markdown 文字

4. **「讀取元數據」節點**：
   - 返回 JSON（title, description, viewcount 等）

5. **「各版本 view」節點**：
   - 輸出 3 個欄位（雙開模式、純編輯模式、純閱讀模式）

---

## ⚠️ 常見錯誤與解決

### 錯誤 1：返回 HTML 頁面

**症狀**：
```json
{
  "statusCode": 200,
  "data": "<!DOCTYPE html>..."
}
```

**原因**：Follow Redirects 沒有關閉

**解決**：
1. 打開「創建筆記」節點
2. 滾動到 Options → Redirects
3. 確認 Follow Redirects = **OFF**（灰色開關）

---

### 錯誤 2：顯示 302 錯誤

**症狀**：
```
NodeApiError: 302 - "Found. Redirecting to..."
```

**原因**：Never Error 沒有啟用

**解決**：
1. 打開「創建筆記」節點
2. 滾動到 Options → Response
3. 確認 Never Error = **ON**（綠色開關）

---

### 錯誤 3：Code 節點報錯

**症狀**：
```
Cannot read property 'location' of undefined
```

**原因**：Include Response Headers and Status 沒有啟用

**解決**：
1. 打開「創建筆記」節點
2. 滾動到 Options → Response
3. 確認 Include Response Headers and Status = **ON**（綠色開關）

---

### 錯誤 4：401 Unauthorized

**症狀**：
```json
{
  "error": "Invalid API Key"
}
```

**原因**：Token 無效或未設置

**解決**：
1. 檢查「Content Parameter」節點的 `token_manager_key`
2. 確認 Token 有效（到 Token Manager 檢查）
3. 確認 Token 的 Scopes 包含 `hedgedoc` 或 `*`

---

### 錯誤 5：匯入後所有節點都是紅色

**症狀**：所有節點顯示錯誤圖標

**原因**：節點類型不相容或 n8n 版本太舊

**解決**：
1. 確認 n8n 版本 >= 1.0.0
2. 更新 n8n 到最新版本
3. 或參考 [n8n-setup-guide.md](../n8n-setup-guide.md) 手動創建

---

### 錯誤 6：curl 可以但 n8n 不行

**症狀**：終端機 curl 測試成功，但 n8n 失敗

**原因**：
- curl `-i` 不跟隨 redirect
- n8n 預設跟隨 redirect

**解決**：
- 確認範本中的 Options 設置正確
- 或查看 [04-troubleshooting/4.4-n8n-hedgedoc-redirect-issue.md](../04-troubleshooting/4.4-n8n-hedgedoc-redirect-issue.md)

---

## 🔍 節點詳細說明

### 節點 1：Manual Trigger

**類型**：Manual Trigger  
**作用**：手動觸發 workflow  
**設置**：無需設置

---

### 節點 2：Content Parameter

**類型**：Set  
**作用**：設置 Token 和筆記內容

**設置**：
```javascript
{
  "Markdown Article": "=# 測試筆記標題\n\n## 測試時間\n{{ $now }}\n\n...",
  "token_manager_key": "YOUR_TOKEN_HERE"  // ← 需要修改這裡
}
```

**需要修改**：
- `token_manager_key` - 改為你的 Token
- `Markdown Article` - 改為你要創建的內容（可選）

---

### 節點 3：創建筆記

**類型**：HTTP Request  
**作用**：調用 Gateway API 創建筆記

**設置**：
```
Method: POST
URL: https://api-gateway.cryptoxlab.workers.dev/api/hedgedoc/new

Headers:
  X-API-Key: ={{ $json.token_manager_key }}
  Content-Type: text/markdown

Body:
  Send Body: ON
  Body Content Type: Raw/JSON
  Content: ={{ $json['Markdown Article'] }}

Options（關鍵！）:
  Redirects:
    ☐ Follow Redirects (關閉)
  
  Response:
    ☑ Include Response Headers and Status (開啟)
    ☑ Never Error (開啟)
```

**輸出**：
```json
{
  "statusCode": 302,
  "headers": {
    "location": "https://api-gateway.cryptoxlab.workers.dev/api/hedgedoc/xxx"
  }
}
```

---

### 節點 4：整理新 markdown 文件輸出參數

**類型**：Code  
**作用**：從 Location header 提取 Note ID 和生成所有 URLs

**代碼**：
```javascript
// 從 HTTP Response 的 headers 中提取 location
const location = $input.item.json.headers.location;

// 從 URL 中提取 Note ID（最後一段）
const noteId = location.split('/').pop();

// 構建不同格式的 URL
return {
  noteId: noteId,
  
  // Gateway URLs（透過 Token Manager）
  gatewayUrl: location,
  downloadUrl: `${location}/download`,
  infoUrl: `${location}/info`,
  
  // 直接訪問 HedgeDoc URLs
  directUrl: `https://md.blocktempo.ai/${noteId}`,
  editUrl: `https://md.blocktempo.ai/${noteId}?edit`,      // 整頁編輯模式
  bothUrl: `https://md.blocktempo.ai/${noteId}?both`,      // 雙開編輯模式
  viewUrl: `https://md.blocktempo.ai/${noteId}?view`,      // 只讀模式
  
  // 其他資訊
  createdAt: new Date().toISOString()
};
```

**輸出**：見上方「範本輸出說明」章節

---

### 節點 5：讀取 markdown 文件內容

**類型**：HTTP Request  
**作用**：讀取剛創建的筆記 Markdown 內容

**設置**：
```
Method: GET
URL: ={{ $json.downloadUrl }}

Headers:
  X-API-Key: ={{ $('Content Parameter').item.json.token_manager_key }}
```

**輸出**：完整的 Markdown 文字內容

---

### 節點 6：讀取元數據

**類型**：HTTP Request  
**作用**：獲取筆記的元數據（標題、時間等）

**設置**：
```
Method: GET
URL: ={{ $json.infoUrl }}

Headers:
  X-API-Key: ={{ $('Content Parameter').item.json.token_manager_key }}
  Accept: application/json
```

**輸出**：
```json
{
  "title": "測試筆記標題",
  "description": "...",
  "viewcount": 0,
  "createtime": "2025-11-09T...",
  "updatetime": null
}
```

---

### 節點 7：各版本 view

**類型**：Set  
**作用**：整理各種訪問模式的 URLs

**設置**：
```
Fields to Set:
  - 雙開模式: ={{ $json.bothUrl }}
  - 純編輯模式: ={{ $json.editUrl }}
  - 純閱讀模式: ={{ $json.viewUrl }}
```

**輸出**：
```json
{
  "雙開模式": "https://md.blocktempo.ai/xxx?both",
  "純編輯模式": "https://md.blocktempo.ai/xxx?edit",
  "純閱讀模式": "https://md.blocktempo.ai/xxx?view"
}
```

---

## 💻 進階自定義

### 修改範本節點

#### 修改筆記內容

在「Content Parameter」節點：

```javascript
"Markdown Article": "=# 你的標題\n\n你的內容..."
```

#### 使用前一個節點的數據

```javascript
"Markdown Article": "={{ $json.articleContent }}"
```

#### 添加更多參數

```javascript
{
  "Markdown Article": "...",
  "token_manager_key": "...",
  "customAlias": "my-note-2025",  // 自定義 alias
  "author": "John Doe"             // 其他元數據
}
```

---

## 🚀 實戰範例

### 範例 1：從 Webhook 接收並創建筆記

**Workflow 設計**：
```
[Webhook Trigger] - 接收 POST 請求
     ↓
[Code] - 將接收的數據轉換為 Markdown
     ↓
[導入此範本] - 創建 HedgeDoc 筆記
     ↓
[Slack/Discord] - 發送通知
```

**Code 節點範例**（轉換為 Markdown）：
```javascript
const article = $input.item.json;

const markdown = `# ${article.title}

**作者**：${article.author}  
**日期**：${new Date().toLocaleDateString()}

---

${article.content}

## 標籤
${article.tags.map(tag => `- ${tag}`).join('\n')}
`;

return {
  articleContent: markdown,
  title: article.title
};
```

---

### 範例 2：RSS 自動轉 HedgeDoc

**Workflow 設計**：
```
[RSS Feed Trigger] - 每小時檢查 RSS
     ↓
[Filter] - 篩選新文章
     ↓
[Code] - 格式化為 Markdown
     ↓
[導入此範本] - 創建 HedgeDoc 筆記
     ↓
[Set] - 記錄到數據庫
```

**Code 節點範例**：
```javascript
const rssItem = $input.item.json;

return {
  articleContent: `# ${rssItem.title}

**來源**：${rssItem.link}  
**發布時間**：${rssItem.pubDate}

---

${rssItem.contentSnippet || rssItem.content}

---

[原文連結](${rssItem.link})
`
};
```

---

### 範例 3：批量創建筆記

**Workflow 設計**：
```
[Manual Trigger/Webhook] - 接收文章列表
     ↓
[Split In Batches] - 分批處理（避免速率限制）
     ↓
[Loop Over Items] - 遍歷每篇文章
     ↓
[導入此範本] - 創建 HedgeDoc 筆記
     ↓
[Wait] - 等待 1 秒（避免速率限制）
     ↓
[Aggregate] - 匯總所有結果
```

---

### 範例 4：定期備份所有筆記

**Workflow 設計**：
```
[Cron Trigger] - 每天凌晨 2:00
     ↓
[HTTP Request] - 讀取筆記列表（從你的資料庫）
     ↓
[Loop] - 遍歷每個筆記 ID
     ↓
[HTTP Request] - 讀取筆記內容（使用範本的讀取配置）
     ↓
[Write Binary File] - 保存到本地/雲端
```

---

## 🆘 疑難雜症排查

### Debug 步驟

#### 1. 查看完整輸出

在節點上右鍵 → "Copy input to clipboard"  
在文本編輯器中查看完整 JSON

#### 2. 使用 n8n 的測試功能

點擊節點 → "Test step"  
查看實際發送的請求和響應

#### 3. 對比 curl 結果

在終端中測試相同的請求：
```bash
curl -X POST https://api-gateway.cryptoxlab.workers.dev/api/hedgedoc/new \
  -H "X-API-Key: your_token" \
  -H "Content-Type: text/markdown" \
  -d "# Test" \
  -i
```

#### 4. 檢查 Gateway 日誌

前往 Token Manager Dashboard  
查看最近的 API 調用記錄

---

### 問題：Token 無法使用

**檢查清單**：
1. Token 是否已撤銷？→ 到 Token Manager 檢查
2. Token 的 Scopes 是否包含 `hedgedoc` 或 `*`？
3. Token 是否已過期？

**在 Token Manager 中檢查**：
```
Token 管理頁面：https://token.blocktempo.ai/tokens
查看：
  - Status: Active / Revoked
  - Scopes: 是否包含 hedgedoc
  - Expires: 是否過期
```

---

### 問題：無法提取 Note ID

**症狀**：
```
Cannot read property 'location' of undefined
```

**原因**：上一個節點沒有返回完整響應

**解決**：
1. 檢查「創建筆記」節點
2. 確認 "Include Response Headers and Status" = ON
3. 執行後檢查輸出是否包含 `headers.location`

**Debug 方法**：
```javascript
// 在 Code 節點開頭添加
console.log('Input:', JSON.stringify($input.item.json, null, 2));

// 檢查 location 是否存在
if (!$input.item.json.headers || !$input.item.json.headers.location) {
  throw new Error('Location header not found');
}
```

---

### 問題：執行時間過長

**症狀**：workflow 執行超過 10 秒

**原因**：
1. 速率限制（HedgeDoc 限制 20 次/時間窗口）
2. 網路延遲
3. Gateway 響應慢

**解決**：
1. 添加 Wait 節點（避免速率限制）
2. 設置 Retry on Fail
3. 檢查 Gateway 狀態：https://token.blocktempo.ai/system-health

---

### 問題：範本無法在不同 n8n 環境使用

**症狀**：在另一個 n8n 實例中匯入失敗

**原因**：環境變數或設置不同

**解決**：
1. 確認目標環境有設置 `HEDGEDOC_TOKEN` 環境變數
2. 或在「Content Parameter」節點直接填入 Token
3. 檢查 n8n 版本是否相容（>= 1.0.0）

---

## 💡 最佳實踐

### 1. 使用環境變數管理 Token

```
❌ 不要：直接寫 Token 在節點中
"token_manager_key": "ntk_SHJtugzk__UyMjpr2rhMeU3NAOV3UhgR1Bj-peq2qqQ"

✅ 建議：使用環境變數
"token_manager_key": "={{ $env.HEDGEDOC_TOKEN }}"
```

**設置環境變數**：
1. 在 n8n 中：Settings → Variables
2. 點擊「Add Variable」
3. Name: `HEDGEDOC_TOKEN`
4. Value: 你的 Token
5. 保存

---

### 2. 保存為你自己的範本

修改後，在 n8n 中：
1. 點擊「⋯」→「Duplicate」
2. 重新命名（例如：「HedgeDoc - 新聞稿發布」）
3. 根據你的需求調整

**好處**：
- 保留原始範本作為參考
- 可以創建多個專門用途的 workflow
- 易於管理和維護

---

### 3. 添加錯誤處理

在「創建筆記」節點後添加 **IF** 節點：

```javascript
// 檢查是否成功創建
{{ $json.statusCode === 302 && $json.headers.location }}
```

**True 分支**：繼續後續處理  
**False 分支**：發送錯誤通知

---

### 4. 添加通知機制

在 workflow 最後添加通知節點：

**成功通知（Slack）**：
```
Message:
✅ HedgeDoc 筆記創建成功！

📝 編輯連結：{{$json.editUrl}}
👀 預覽連結：{{$json.viewUrl}}
🔗 分享連結：{{$json.directUrl}}

Note ID: {{$json.noteId}}
創建時間：{{$json.createdAt}}
```

**失敗通知（Email/Slack）**：
```
❌ HedgeDoc 筆記創建失敗

錯誤訊息：{{$json.error}}
請檢查 Token 和 Gateway 狀態
```

---

### 5. 定期測試

建議每週執行一次測試，確保：
- Token 仍然有效
- Gateway 正常運作
- HedgeDoc 服務可用

**創建測試 workflow**：
```
[Cron Trigger] - 每週一 09:00
     ↓
[使用此範本] - 創建測試筆記
     ↓
[IF] - 檢查是否成功
     ↓
[Slack] - 發送測試結果報告
```

---

### 6. 速率限制管理

HedgeDoc 預設：20 次請求 / 時間窗口

**批量創建時**：
```
[Split In Batches] 
  Batch Size: 10
     ↓
[Loop]
     ↓
[HTTP Request] - 創建筆記
     ↓
[Wait] - 等待 3 秒
```

**檢查剩餘次數**：
```javascript
// 從 headers 中讀取
const remaining = $json.headers['x-ratelimit-remaining'];
const limit = $json.headers['x-ratelimit-limit'];

console.log(`剩餘次數：${remaining}/${limit}`);
```

---

## 🎓 進階技巧

### 技巧 1：動態生成 Alias

```javascript
// 在 Content Parameter 之前添加 Code 節點
const timestamp = new Date().toISOString().split('T')[0];  // 2025-11-09
const title = $json.title.toLowerCase().replace(/\s+/g, '-');
const alias = `${title}-${timestamp}`;

return {
  customAlias: alias,
  ...($json)
};
```

然後在創建筆記時使用：
```
URL: https://api-gateway.cryptoxlab.workers.dev/api/hedgedoc/new/{{$json.customAlias}}
```

---

### 技巧 2：批次處理大量文章

使用 **Split In Batches** 節點：

```
[HTTP Request] - 獲取文章列表
     ↓
[Split In Batches]
  Batch Size: 5
  Options: Keep Input Data = ON
     ↓
[Loop Over Items]
     ↓
[創建筆記]
     ↓
[Wait] - 2 seconds
```

---

### 技巧 3：錯誤重試機制

在「創建筆記」節點的 Options 中：

```
☑ Retry on Fail
  Max Retries: 3
  Wait Between Retries: 1000 (ms)
  Retry on HTTP Response Codes: 429,500,502,503,504
```

---

### 技巧 4：記錄所有操作

添加 **Set** 節點記錄重要資訊：

```
Fields to Set:
  - log: Created note {{$json.noteId}} at {{$now}}
  - noteUrl: {{$json.directUrl}}
  - status: success
```

然後發送到：
- Database（持久化記錄）
- Google Sheets（追蹤報表）
- Airtable（協作管理）

---

## 📚 完整的 n8n 節點設置參考

### HTTP Request 節點（創建筆記）- 完整配置

```
Method: POST
URL: https://api-gateway.cryptoxlab.workers.dev/api/hedgedoc/new
Authentication: None

Send Query Parameters: OFF

Send Headers: ON
Header Parameters:
  - Name: X-API-Key
    Value: ={{ $json.token_manager_key }}
  - Name: Content-Type
    Value: text/markdown

Send Body: ON
Body Content Type: Raw/JSON
Content Type: text/markdown
Body: ={{ $json['Markdown Article'] }}

Options:
  Redirects:
    ☐ Follow Redirects
  
  Response:
    ☑ Include Response Headers and Status
    ☑ Never Error
  
  Response Format: Autodetect
  
  Timeout: 30000 (可選)
  
  Retry on Fail: (可選)
    ☑ Enable
    Max Retries: 3
    Wait Between Retries: 1000
```

---

### Code 節點 - 完整配置

**Mode**: Run Once for All Items

**JavaScript 代碼**：
```javascript
// 從 HTTP Response 的 headers 中提取 location
const location = $input.item.json.headers.location;

// 從 URL 中提取 Note ID（最後一段）
const noteId = location.split('/').pop();

// 構建不同格式的 URL
return {
  noteId: noteId,
  
  // Gateway URLs（透過 Token Manager）
  gatewayUrl: location,
  downloadUrl: `${location}/download`,
  infoUrl: `${location}/info`,
  
  // 直接訪問 HedgeDoc URLs
  directUrl: `https://md.blocktempo.ai/${noteId}`,
  editUrl: `https://md.blocktempo.ai/${noteId}?edit`,      // 整頁編輯模式
  bothUrl: `https://md.blocktempo.ai/${noteId}?both`,      // 雙開編輯模式
  viewUrl: `https://md.blocktempo.ai/${noteId}?view`,      // 只讀模式
  
  // 其他資訊
  createdAt: new Date().toISOString()
};
```

---

## 🔗 相關資源

- **範本文件**：[workflows/hedgedoc-create-and-read-template.json](../../workflows/hedgedoc-create-and-read-template.json)
- **workflows 目錄**：[workflows/README.md](../../workflows/README.md)
- **n8n 手動設置指南**：[n8n-setup-guide.md](../n8n-setup-guide.md)
- **curl 快速參考**：[hedgedoc-quick-start.md](../hedgedoc-quick-start.md)
- **問題排查**：[04-troubleshooting/4.4-n8n-hedgedoc-redirect-issue.md](../04-troubleshooting/4.4-n8n-hedgedoc-redirect-issue.md)
- **Token Manager 指南**：[token-manager-guide.md](../token-manager-guide.md)
- **測試報告**：[GATEWAY-TEST-REPORT.md](../../GATEWAY-TEST-REPORT.md)

---

**最後更新**：2025-11-09  
**範本版本**：v1.0  
**測試狀態**：✅ 已完整驗證
